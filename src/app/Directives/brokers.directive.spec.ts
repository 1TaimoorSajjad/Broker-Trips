import { BrokersDirective } from './brokers.directive';

describe('BrokersDirective', () => {
  it('should create an instance', () => {
    const directive = new BrokersDirective();
    expect(directive).toBeTruthy();
  });
});
